<!--
  An XML Catalog that rewrites any reference to a file at a URI
  ending with 'htmlmathml-f.ent' to a similarly named file in the
  local directory.
-->
<catalog  xmlns="urn:oasis:names:tc:entity:xmlns:xml:catalog">
  <systemSuffix systemIdSuffix="htmlmathml-f.ent" uri="htmlmathml-f.ent"/>
</catalog>
